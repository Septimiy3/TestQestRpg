<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.simpleproject.testqestrpg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".location.dungeon.DungeonLvlOne"></activity>
        <activity android:name=".PartOne" />
        <activity android:name=".SettingMenu" />
        <activity android:name=".dialogbarmenpartone.DialogBarmenPartOne" />

        <service android:name=".fonSound.BirdSound" />
        <service android:name=".fonSound.BackgroundMusic" />
        <service android:name=".fonSound.NewGameFonMusic" />
        <service android:name=".fonSound.NewGameFonSound" />

        <activity android:name=".NewGame" />
        <activity android:name=".WelcomActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BackstoryActivity" />
        <activity android:name=".MenuActivity" />
    </application>

</manifest>